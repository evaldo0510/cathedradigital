name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Bun
      uses: oven-sh/setup-bun@v1
      with:
        bun-version: latest

    - name: Install dependencies
      run: bun install

    - name: Typecheck
      id: typecheck
      run: |
        echo "Running tsc --noEmit..."
        bun run typecheck > tsc_output.log 2>&1
        RESULT=$?
        cat tsc_output.log
        exit $RESULT

    - name: Lint
      id: lint
      run: |
        echo "Running eslint..."
        bun run lint > eslint_output.log 2>&1
        RESULT=$?
        cat eslint_output.log
        exit $RESULT

    - name: Generate SEO Files
      run: |
        echo "Generating sitemap and robots.txt..."
        bun run scripts/generate-sitemap.ts > sitemap_gen.log 2>&1
        RESULT=$?
        cat sitemap_gen.log
        exit $RESULT

    - name: Validations
      run: |
        EXIT_CODE=0
        
        echo "Validating sitemap..."
        bun run scripts/validate-sitemap.ts > sitemap_val.log 2>&1 || { cat sitemap_val.log; EXIT_CODE=1; }
        
        echo "Validating catechism..."
        bun run scripts/validate-catechism.ts > catechism_val.log 2>&1 || { cat catechism_val.log; EXIT_CODE=1; }
        
        echo "Running security audit..."
        bun run scripts/security-audit.ts > security_audit.log 2>&1 || { cat security_audit.log; EXIT_CODE=1; }
        
        exit $EXIT_CODE

    - name: Build
      run: |
        echo "Running vite build..."
        bun run build > build_output.log 2>&1
        RESULT=$?
        cat build_output.log
        exit $RESULT

    - name: Upload Logs as Artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ci-logs
        path: |
          *.log
          scripts/logs/*.log
