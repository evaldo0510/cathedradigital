name: Mobile CI Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  mobile-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run Mobile E2E Tests (Matrix)
        run: |
          # Simulação de matriz de dispositivos
          # No futuro: npx playwright test --project=iPhone-12 --project=Pixel-5
          bun test src/test/MobileNavigation.test.tsx src/test/BottomNavRegression.test.tsx
        env:
          TEST_VIEWPORTS: "320x568,375x667,414x896,768x1024"
          CI: true

      - name: Upload Test Reports & Screenshots on Failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: mobile-e2e-failure-artifacts
          path: |
            test-results/
            playwright-report/
            screenshots/
            logs/

      - name: Notify CI Failure
        if: failure()
        run: |
          PAYLOAD=$(cat <<EOF
          {
            "text": "🚨 *CATHEDRA CRITICAL: Mobile E2E Failure*",
            "attachments": [
              {
                "fallback": "Mobile navigation tests failed.",
                "color": "#D42020",
                "title": "Mobile Regression Detected",
                "title_link": "${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}",
                "fields": [
                  { "title": "Commit", "value": "${GITHUB_SHA:0:7}", "short": true },
                  { "title": "Branch", "value": "${GITHUB_REF_NAME}", "short": true },
                  { "title": "Pipeline ID", "value": "${GITHUB_RUN_ID}", "short": false },
                  { "title": "Evidence Summary", "value": "Screenshots and Stack Traces attached to the Workflow Run.", "short": false }
                ],
                "footer": "AD MAIOREM DEI GLORIAM — Automated Stability Gate",
                "ts": $(date +%s)
              }
            ]
          }
          EOF
          )
          curl -X POST -H 'Content-type: application/json' --data "$PAYLOAD" ${{ secrets.CI_NOTIFICATION_WEBHOOK }}





