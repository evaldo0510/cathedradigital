/**
 * Sprint UX · Área do Usuário — seção Diário.
 * Reusa `SpiritualJournalPage` (todo o CRUD, pesquisa e estatísticas já existem lá).
 */
import SpiritualJournalPage from "@/components/cathedra/SpiritualJournalPage";

export default function DiarioSection() {
  return <SpiritualJournalPage />;
}
