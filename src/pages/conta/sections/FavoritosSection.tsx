/**
 * Sprint UX · Área do Usuário — seção Favoritos.
 * Reusa a página existente `/profile/favorites`.
 */
import ProfileFavoritesPage from "@/pages/ProfileFavoritesPage";

export default function FavoritosSection() {
  return <ProfileFavoritesPage />;
}
