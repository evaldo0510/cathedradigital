import React from 'react';
import BlockPlaceholder from '../_BlockPlaceholder';

const News: React.FC = () => (
  <BlockPlaceholder
    priority="P6"
    title="Novidades"
    hint="Avisos institucionais · máx. 1 por sessão · aparece apenas quando houver"
  />
);

export default News;
